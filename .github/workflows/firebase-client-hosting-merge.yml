# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy client to Firebase Hosting on merge
"on":
  push:
    branches:
      - master
env:
  VITE_FIREBASE_API_KEY: ${{secrets.VITE_FIREBASE_API_KEY}}
  VITE_FIREBASE_AUTH_DOMAIN: ${{secrets.VITE_FIREBASE_AUTH_DOMAIN}}
  VITE_FIREBASE_PROJECT_ID: ${{secrets.VITE_FIREBASE_PROJECT_ID}}
  VITE_FIREBASE_STORAGE_BUCKET: ${{secrets.VITE_FIREBASE_STORAGE_BUCKET}}
  VITE_FIREBASE_MESSAGING_SENDER_ID: ${{secrets.VITE_FIREBASE_MESSAGING_SENDER_ID}}
  VITE_FIREBASE_APP_ID: ${{secrets.VITE_FIREBASE_APP_ID}}
  VITE_BACKEND_BASE_URL: ${{secrets.VITE_BACKEND_BASE_URL}}
  VITE_FIREBASE_MEASUREMENT_ID: ${{secrets.VITE_MEASUREMENT_ID}}
  VITE_ENV_STRIPE_PUBLISHABLE_KEY: ${{secrets.TEST_STRIPE_PK}}
  VITE_ENV: production
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install volta
        uses: volta-cli/action@v4

      - run: yarn workspaces focus client && yarn workspace client build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_UASC_CEEBC }}"
          channelId: live
          projectId: uasc-ceebc
          entryPoint: client
