# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy Client Staging
on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Run Setup Composite Action
        uses: ./.github/ci-actions/setup/

  deploy-client-staging:
    name: Deploy Client Staging
    needs: [setup]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      checks: write
    environment:
      name: client-staging
      url: ${{ vars.DEPLOYMENT_URL }}
    concurrency:
      group: deploy-client-staging
      cancel-in-progress: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Run Client Deployment Composite Action
        uses: ./.github/cd-actions/deploy-client/
        with:
          strategy: firebase
          environment: staging
          # Firebase/Common Environment Variables
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          BACKEND_BASE_URL: ${{ secrets.BACKEND_BASE_URL }}
          STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PK }}
          SANITY_PROJECT_ID: ${{ secrets.SANITY_PROJECT_ID }}
          # Firebase Deployment Secrets
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_UASC_CEEBC }}
          FIREBASE_PROJECT_ID_DEPLOY: uasc-ceebc
          # Common Secrets
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
