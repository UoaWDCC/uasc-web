name: Prettier
on:
  pull_request:
  push:
    branches:
      - master
jobs:
  Prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install volta
        run: curl https://get.volta.sh | bash

      - run: sudo yarn
      - run: yarn prettier:ci
