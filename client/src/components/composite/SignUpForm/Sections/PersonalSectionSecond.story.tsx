import type { Meta, StoryObj } from "@storybook/react"
import { PersonalSectionSecond } from "./PersonalSection"
const meta: Meta<typeof PersonalSectionSecond> = {
  component: PersonalSectionSecond
}

export default meta

type Story = StoryObj<typeof meta>

export const Default: Story = {}
