/*
import { updatePassword } from "firebase/auth"

updatePassword().then(()=>{

}).catch((error)=>{

}) */
import { PasswordSetup } from "components/generic/PasswordSetup/PasswordSetup"

const AccountSetupSection = () => {
  return <PasswordSetup />
}

export default AccountSetupSection
